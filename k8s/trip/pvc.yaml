apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trip-pvc
  labels:
    app: trip
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
